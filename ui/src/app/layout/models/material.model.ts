export class Material {
  // PANEL ARTÍCULO
  key: string;
  ourKey: string;
  type: number;
  cia: number;
  client: number;

  // <!--PANEL MEDIDAS-->
  long: number; // Largo
  width: number; // Ancho
  height: number; // Alto
  slots: number; // Ranuras
}
